/**
 * Planning Engine types - Defines implementation plans generated by comparing Design Graph with Project Graph.
 */
export interface ImplementationPlan {
    id: string;
    designGraphId: string;
    projectGraphId: string;
    createdAt: string;
    status: 'pending' | 'in_progress' | 'completed' | 'failed';
    totalTasks: number;
    completedTasks: number;
    tasks: Task[];
    summary: PlanSummary;
}
export interface PlanSummary {
    widgetsToCreate: number;
    widgetsToUpdate: number;
    widgetsToReuse: number;
    filesToModify: string[];
    filesToCreate: string[];
    routesToRegister: string[];
    assetsToAdd: string[];
}
export type TaskType = 'create_widget' | 'update_widget' | 'reuse_widget' | 'update_route' | 'add_asset' | 'update_theme' | 'update_localization';
export interface Task {
    id: string;
    type: TaskType;
    priority: 'high' | 'medium' | 'low';
    order: number;
    title: string;
    description: string;
    designNodeId?: string;
    existingWidgetId?: string;
    targetFilePath: string;
    requiresManualReview: boolean;
    dependencies: string[];
    status: 'pending' | 'in_progress' | 'completed' | 'skipped';
}
export interface WidgetMatch {
    designComponentId: string;
    existingWidgetId: string;
    matchScore: number;
    matchReason: MatchReason;
    needsModification: boolean;
    modifications?: Modification[];
}
export type MatchReason = 'exact_match' | 'structural_similarity' | 'semantic_match' | 'partial_overlap';
export interface Modification {
    property: string;
    currentValue?: string;
    requiredValue: string;
    complexity: 'simple' | 'moderate' | 'complex';
}
export interface DesignToCodeMapping {
    designNodeId: string;
    codeWidgetId: string;
    filePath?: string;
    confidence: number;
}
//# sourceMappingURL=planning.engine.d.ts.map